// HTML SECTIONS
const main = document.getElementById("main");
// NAVIGATION
var nav = 0;

// POSTINGS
update_array = []; //lists the current progress.
error_array = []; //lists the errors/mistakes made by the user and in the end posts them into a database so as to make the user revise them later
